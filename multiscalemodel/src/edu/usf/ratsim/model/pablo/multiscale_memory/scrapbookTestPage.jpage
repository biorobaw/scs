// this page is for testing short snippet code
//example 2+2

IntStream cannot be resolvedjava.util.stream.IntStream.range(0,10);
java.util.stream.IntStream.range(1, 5).reduce(1, (x, y) -> x * y)

var stream1 = java.util.stream.DoubleStream.of(3,2, 5.1,1,4);
stream1.map(Math::floor).toArray();
stream1.toArray();An exception occurred during evaluation:  java.lang.IllegalStateException

-1 %3(int) -1